<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tpa_android_decomics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReadComicActivity"></activity>
        <activity android:name=".UploadChapterActivity" />
        <activity android:name=".UploadExistComicActivity" />
        <activity android:name=".UploadNewComicActivity" />
        <activity android:name=".FollowingActivity" />
        <activity android:name=".OtherUserActivity" />
        <activity android:name=".ComicChapterActivity" />
        <activity android:name=".BottomNavigationActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>